# 接一个新 Agent 的手册

要把手机接上一个**已经在跑**的 AI 会话，剥到底只有两件事：**读得到**、**写得进**。

读（出站）几乎总是能做——扫会话日志文件而已。**难点全在写。** 这份手册记的是
从零逆出来的那套路，Claude 只是走通了的那个例子。

每节末尾的「**通用**」是可以直接搬到下一个 Agent 的；其余是 Claude 的具体实现，
当成样板看。

---

## 0. 先分清「读」和「写」不是一回事

很多人一上来就想"怎么把消息发进去"，忽略了**读**其实也有一堆坑。两件事的难度
差一个数量级，别混在一起做。

| | 难度 | 为什么 |
|---|---|---|
| 读 | 低 | 日志文件就在硬盘上，格式自己摸 |
| 写 | **高** | 要骗过一个正在跑的进程，让它把外来文本当成一条正常的输入 |

先做读。读通了你就有了一个能用的东西（只读的旁观窗口），再啃写。

---

## 1. 找入站通道：按这个顺序试

别一上来就想着模拟键盘。从"最正规"往"最脏"排：

0. **★ 先翻这个 App 自己的 CLI 子命令，看有没有一条已经替你说话的路。**
   这一步**要排在逆协议前面**——逆一个 socket 可能好几天，而厂商很可能早
   就留好了门：Codex 就是 `codex queue --thread <id> --message <text>`。
   翻 `--help` 的**全部子命令**（`codex --help` 里 `queue` 就明明白白列着），
   别只挑名字像"远程"的看。
   **踩过的坑**：我们花在 `ipc.sock` 上的功夫，一大半是白花的——那个 socket
   确实认对端身份进不去，但**根本不需要进去**：`queue` 就是官方开的正门，
   而且不用改任何持久配置（比 `enable-remote-control` 干净）。
1. **官方有没有远程控制开关。**
   Claude Code 有 `/remote` 那一套；Codex 有
   `codex app-server daemon enable-remote-control`。
   坑：Claude 的远程控制在 `ANTHROPIC_BASE_URL` 不是官方地址时**直接不可用**
   （自定义端点的用户等于没有）；Codex 那条会**改持久配置**，而且本机没有
   managed daemon 时根本没用上。有开关不等于你能用，实测。
2. **有没有本地 socket / IPC。**
   Claude Code：`/tmp/cc-socks/<pid>.sock`（UDS，换行分隔 JSON）。
   Codex：`~/.codex/ipc/ipc.sock`——**认对端身份**，见 §2.4。
   这是最理想的一类——没有权限问题，不抢焦点。**但先确认它是不是需要你自己
   实现协议**（Codex 那条：不是，用它的 CLI 就行）。
3. **文件监听。** 有的 agent 会盯某个文件/目录当输入队列。翻它的配置和进程
   持有的 fd（`lsof -p <pid>`）找线索。
4. **兜底：合成键盘事件**（`osascript` + System Events，或 Linux 的
   `xdotool`/`ydotool`）。通用、脆、要辅助功能权限、窗口必须在最前、焦点错了
   会打进别的窗口。**这是最后手段。**

> **通用**：先翻**这个 App 自己的 CLI**，再翻官方远程控制，再翻本地 IPC，
> 最后才碰模拟输入。顺序反了会白干很多天。

### 1.1 ★ 但还有一条最干净的路：自己起进程，直接拿 stdin

> **这一节是"通用手法"，不是"phone-bridge 现在的行为"。** 基于它做的那条路
> （claude.py 的 `ALLOW_SPAWN`）**默认关着**——它技术上成立，实现上也通了，
> 但**用起来不值**：语义说不圆（是"另起一个脑子"，不是接管）、要拿祖先的凭据、
> 还会跟桌面窗口抢同一份记录（§1.1 事实 8）。skill 现在只留"在跑的能发、
> 没在跑的只读"。下面这些实测结论留着——接**别的** App 时，这条路该试还得试。

上面 1–4 都是想办法**接进**一个已经在跑的会话。可如果你要的不是"接管那个窗口"，
而是"能跟**任何一个**对话说话"，那根本不用接——**自己生一个**：

```bash
claude --resume <sessionId> --input-format stream-json --output-format stream-json
```

进程是你生的，**stdin 就是你的**。想发什么发什么：**没有会话树认证、没有 socket、
什么都不用逆向**。linco 和 Lody 走的都是这条路（ACP 的定义就是"本地 agent 作为
编辑器的子进程运行"）。再加 `--permission-prompt-tool stdio`，权限提示也能接到手机上。

**代价，必须先想清楚：这是"新建一个脑袋"，不是"接管那个已经在跑的"。**

| | 接管（UDS 注入） | 自起（拿 stdin） |
|---|---|---|
| 打到**正在跑的桌面会话** | **能** | 不能 |
| 打到**任意历史对话** | 不能 | **能**（`--resume` 拉起来） |
| 同时管几个 | **只能一个**（见 §2.3） | 想几个几个 |
| 要逆向吗 | 要 | 不要 |
| 桌面窗口跟着动吗 | 跟着动 | **不跟**，两边各写各的 |

**这两条不是优劣，是两种产品。** 别拿一条的标准去否定另一条。

#### 实测（2.1.273，2026-09-16）：这条路的三个硬事实

1. **不另建文件，也不换 sessionId。** `--resume <sid>` 不论交互式还是
   `--input-format stream-json`，都是往**原来那个** `.jsonl` 追加。
   实测 3 行 → 22 行，项目目录下**没有**新文件出现，流里回的 `session_id`
   还是原来那个。（所以别指望"自起"能跟"接管"隔离开——**它们是同一个写入者
   在内讧**，上表"两边各写各的"那句要按这个理解。）
2. **它自己也会开 socket、也会进注册表。** init 行里带着
   `messaging_socket_path`，`~/.claude/sessions/<pid>.json` 跟着多一条。
   于是**同一个 sessionId 会对应两个 pid**。任何 `sessionId -> pid` 的字典
   都会静默挑错一个——发现层要按 pid 存，或者优先挑"已经绑在桌面窗口上"
   的那一个。这个坑不会报错，只会把消息投给随机一边。
3. **认证靠环境。** 从会话树里 spawn，`settings.json` 的 `env` 天然继承，
   `apiKeySource` 正常、能真出话；从干净环境（沙箱、CI、cron）spawn，
   会直接 `Not logged in · Please run /login`，而**进程照样起得来、照样注册
   socket**，看起来一切正常。spawn 之前先确认凭据在环境里。

#### 实测（2026-09-16，落地版）：把这条路真跑通，又踩了三个

上面那三条是"这条路存在"的证据。真把它接成产品（前端对**任何**没在跑的对话
自动起一个）时，又踩出三条，每一条的症状都长得像别的毛病：

4. **`--verbose` 不是可选的。** 不给它，CLI 当场拒掉整个 stream-json 输出：
   `Error: When using --print, --output-format=stream-json requires --verbose`。
   阴的地方是**它先写两行 init 元数据再死**——日志文件确实变长了、进程确实起来过，
   看起来"发送成功"，其实一个字都没进去。**验证要看"有没有 assistant 条目"，
   不是"文件长没长"。**
5. **子进程拿不到凭据，harness 把它剥掉了。** 实测：会话进程的环境里有
   `ANTHROPIC_AUTH_TOKEN`，而**从它派生的一切**（Bash 工具、后台任务、我们起的前端）
   都只剩 `ANTHROPIC_BASE_URL`。于是自起的会话报 `Not logged in · Please run /login`
   ——更糟的是**这句抱怨是当成一条 assistant 回复写进用户对话里的**，凭空污染历史。
   两个后果：发之前先确认凭据到手（没有就别发，宁可不发）；以及 `settings.json` 里的
   `ANTHROPIC_API_KEY` 在这台机器上**没用**（自建网关回 401：它要 `Authorization`
   头，不吃 `x-api-key`）。能用的那份只在会话进程自己的环境里，得去那儿拿。
6. **stdin 一关（EOF）它就自己退。** 这跟 §2.1「子进程不会随父进程死」**刚好相反**，
   别搞混：§2.1 说的是"没有 stdin 关系"的常驻服务，这里是"输入流是它的命"。实测
   杀掉前端，自起的会话 3 秒内自己退干净——所以**不用担心攒下一堆孤儿**，也就不需要
   落盘记录、启动清理那一套。
7. **自起那条路别再套跨会话信封。** 注入进**别人的桌面会话**时必须套（宿主 UI 靠它
   署名、靠它把这句话标成"来自外部"，见 §5），可自起的会话**没有别的宿主 UI**——
   套上去只会让模型把**用户自己打的**话当成"另一个会话发来的消息"。实测踩过：那个
   agent 于是花掉几轮，用它的跨会话工具去**回复发送方**，回复落到了另一个会话里，
   而用户手机上那一页什么都看不到。**喂给模型的输入要按"它其实是什么"来写。**

8. **"窗口是后来才开的"你防不住，只能事后收。** "有活进程就不自起"这个判据是在
   **发消息那一瞬间**看的；用户完全可能在那之后十几秒才把那个对话在电脑上打开。
   实测：自起的进程和桌面窗口的进程相差 **17 秒**，两个都活着、读同一份记录。

   但**"电脑看不到手机上说的话"的根因不是 fork**，先别往那儿想——是**窗口只在
   "打开的那一刻"把文件读一次，之后不回读**：

   | 谁 | 什么时候读文件 |
   |---|---|
   | 手机端 | **每次轮询都重读** → 谁写的都看得见，永远最新 |
   | 桌面窗口 | **只在打开那一刻读一次** → 之后写的，谁都看不见 |

   所以那是一件**跟"谁写的"无关**的事：CLI 写的、我们自起的 fork 写的、手机写的，
   对**已经开着**的窗口一视同仁都看不见。**实测（2026-09-16）**：用户当时报"手机
   上发的电脑看不到"，后来把那个对话**关掉重开**（正赶上 App 重启），那一对就出现了
   ——**包括我们 fork 写的那对**；同一时间他用 CLI 发的一轮，已经开着的老窗口同样
   没显示。所以"fork 比 CLI 差在哪"这个问题的答案是**不差**；差的是"你什么时候打开
   那个窗口"。**判据也改成这个**：跟谁写的无关，只跟"那个窗口是什么时候开的"有关。

   仍然要收，因为两个脑袋会**分叉**：各自只吃自己收到的输入，于是"窗口里的上下文"
   和"记录里的内容"不是一回事。所以发现旁边冒出**外人**就立刻收掉自己（忙的时候等
   它说完，别把回复截断）——不然更阴的是：用户把窗口一关，下次手机发消息会**复用**
   那个还活着的脑袋，而它的上下文停在开窗口之前，等于对着一段过期记忆说话。

> **通用**：`--resume` 类接口的语义要先实测清楚是"追加"还是"派生"。
> 追加的话，你自起的那个脑袋和原来的进程是**抢同一支笔**，不是各写一份。
> 另外**"进程起来了"和"话进去了"是两件事**：前者看 `ps`，后者只能去会话日志里
> 数 assistant 条目。还有：**壳是给"接收方的渲染器"用的**。没有那个渲染器的时候，
> 壳不是无害的装饰，而是会让模型误判来源的噪声——同一个 payload，走的通道不同，
> 该长得不一样。最后：**"当时没在跑"是个瞬间判断，不是一个状态**——用户随时能在
> 你判断之后把那个东西打开。所以"临时代班"必须配上"发现原主回来了就让位"的收尾，
> 否则你留下的是个**跟用户正看着的东西抢同一份记录**的幽灵。

---

## 2. ★ socket 找得到 ≠ 发得进去。先验认证方式

**这一步错了，你会得到一个完全静默的失败**——这是整个项目里最贵的坑。

Claude Code 的入站只接受**会话进程的子孙进程**。别的进程照样能连上 socket、
照样收到 `{"ok": true}`，**而帧被直接丢弃**。没有报错，没有日志，消息石沉大海。

踩坑现场：用 `nohup ... &` 起 bridge，它被 reparent 到 launchd（`ppid=1`），
于是注入全废，而界面上显示"可发送"。

**所以：`can_send` 不许只检查"socket 文件在不在"，必须真去查自己的祖先链。**

```python
def _my_ancestors():
    """我自己往上走到 launchd 的那一串 pid。"""
    out, pid = set(), os.getpid()
    for _ in range(20):
        ppid = int(subprocess.check_output(
            ["ps", "-o", "ppid=", "-p", str(pid)]).strip() or 0)
        if ppid <= 1:
            break
        out.add(ppid)
        pid = ppid
    return out
```

会话进程的 pid 必须在这个集合里，`can_send` 才返回 `True`。

**怎么发现的**：先做一次"故意失败"的对照实验——同一段代码，一次从会话内起、
一次 `nohup` 起，对比注入结果。前者进对话，后者什么都不进。别靠读代码猜认证。

> **通用**：本地 IPC 常见三类认证——进程树关系、文件权限/属主、对端身份。
> Claude 是**进程树**。Codex 那条是**对端身份**（看着像握手，其实是认人是
> 谁——§2.4）。
> **验证方法只有一个：真发一条，看它有没有进对话。** socket 返回 ok 什么都证明不了。

### 2.0 ★ 边界到底画在哪：实测矩阵（2.1.273，2026-09-16）

本节结论更正了 §2 那句过于笼统的"必须是子孙"。准确的说法是：
**认证的严格程度跟着「收信的那个会话」走，不跟着「发信人」走。**

| 收信会话 | 发信人 | 结果 |
|---|---|---|
| 桌面 App 交互式会话（**busy**，正在跑一轮） | 树外孤儿（`ppid=1`） | ❌ **静默丢弃** |
| 桌面 App 交互式会话（**idle**） | 树外孤儿（`ppid=1`） | ❌ **静默丢弃** |
| 另一个**空闲**的桌面交互式会话 | 树外兄弟进程 | ❌ **静默丢弃** |
| 自起 headless（2.1.273） | 同 shell 的兄弟进程 | ✅ 进 |
| 自起 headless（2.1.273） | 树外孤儿（`ppid=1`） | ✅ 进 |
| 自起 headless（**2.1.271，App 自带那份二进制**） | 同 shell 的兄弟进程 | ✅ 进 |

**判据是「收信那个会话是不是交互式的」，其余全不是变量。**
交互式桌面会话一律查血统；我们自己 spawn 出来的 headless（`--input-format
stream-json`）会话根本不查。同一个目标、同一份协议、只换发信人，都进去了——
说明那几行之间根本没有"血统"这个变量在起作用。

**两个以为有关、实测无关的变量**，值得记下来因为它们都很像原因：

- **不是"忙"**。同一个桌面会话等它**空闲**了再发一次，照样丢。
- **不是版本**。桌面 App 自带的是 `2.1.271`，我测 CLI 用的是 `2.1.273`，
  一度以为差异出在这儿。于是**用 App 自带的那个二进制**起了个 headless 会话
  ——树外注入**照进**。同一个版本，headless 收、交互式不收。

二进制里的符号佐证这个方向（2.1.273 的 `bin/claude.exe` 字符串表）：
`readAncestors` / `selfSentAncestry` / `childTokenPresented` / `verdictOf` /
`no-evidence` / `not-self`，再加 `authRequired` / `authOkReported` /
`authDropReported` / **`silentDropReported`**——最后那个名字说明"静默丢弃"是有
独立计数器的，和 §2 描述的现象对得上。判定确实是"读连接方的祖先链"，也有
"子令牌"这条降级路径。**但实测显示：这条判定在非交互会话上不生效。**

**没排掉的一个变量**：那次桌面探针是在会话 **busy**（正在跑一轮）时发的。
"忙"是否也参与判定，还没验——要找它空闲的窗口重发一次才知道。

**资格是「一个中继只对一个会话有效」**——这才是这个限制真正扎手的地方。
bridge 是会话 X 的子孙，那它就只在 X 里有资格；同一个 bridge 拿去写会话 Y，
照样静默丢弃。所以**想看住 N 个桌面会话，就得有 N 个各待在自己树里的中继**，
不能用一个通吃。（headless 会话那条"不查"是个例外：它给了一条不用进树
就能写进去的路，见 §1.1。）

**我这次自己的踩坑记录**：给桌面会话灌探针，`connect OK` + `sendall OK`，
我据此宣布"通了"——**错了**。回转录里找，那条消息根本不存在。
`connect`/`sendall` 成功只证明"管道收下了"，不证明"帧被采纳"。
跟前两段写的一字不差，我还是踩了。

> **通用**：判认证只有一招——**真发一条，回头去收信方的会话日志里翻**。
> 找不到就是被丢了，别信任何返回码。而且要找**所有类型**的条目：目标忙的时候
> 跨会话消息会落成 `attachment` / `queued_command`（见 §6.1），
> 只 grep `type=="user"` 会漏掉一半。

### 2.1 子进程不会随父进程死

顺带的坑：父进程一没，子进程被 launchd 收养然后**继续活着**。所以"绑定会话
生命周期"不能靠"父死了子跟着死"，得自己盯：

```python
def _watch_session(stop_event):
    start_ppid = os.getppid()
    if start_ppid == 1:            # 起手就是孤儿，注定发不进去
        print("[bridge] 警告：启动时父进程就是 launchd（孤儿）")
        return
    while not stop_event.wait(5):
        if os.getppid() != start_ppid:     # 被收养了 = 会话没了
            print("[bridge] 会话进程没了，bridge 跟着退出")
            stop_event.set()
            return
```

> **通用**：任何"挂在宿主进程上"的服务都得自己实现这一步。`ppid` 变化是唯一
> 可靠的信号。

### 2.2 ★ 别用命令行反推会话身份——去找宿主的注册表

**症状**：手机列表上，老对话有绿点，**刚新建的那个永远没有**。

**我第一版怎么写的**：`ps -axo pid=,command=` 扫出所有 claude 进程，从
`--resume=<sessionId>` 里正则抠出 sessionId 来。看着很聪明，**漏了最要紧的一种情况**：

> **全新会话的命令行里根本没有 `--resume=`。** 没东西可 resume 嘛。
> 于是它扫不到 → 判断成"没在跑" → 永远灰的。

这个 bug 特别阴：你测的时候手上总有一个聊过几轮的老会话（有 `--resume=`），
一切正常；只有"刚点新建"这一条路径是坏的，而那恰好是最自然的用法。

**正确做法**：宿主自己会维护一份注册表，去读它，别猜。

```
~/.claude/sessions/<pid>.json
{"pid":7602,"sessionId":"48a01713-…","cwd":"…","status":"idle",
 "messagingSocketPath":"/tmp/cc-socks/7602.sock","name":"codex-45", …}
```

sessionId、pid、cwd、socket 路径、**忙/闲状态**，全在里面，还是宿主自己写的
（不用你从日志里推）。拿 `os.kill(pid, 0)` 过一遍确认进程还活着就够了。
实测这个目录是自清理的——会话退出，文件跟着没。

```python
for path in glob.glob(os.path.join(SESSIONS_DIR, "*.json")):
    info = json.load(open(path))
    if _pid_alive(info["pid"]):
        found[info["sessionId"]] = info["pid"]
```

> **通用**：`ps` / 命令行参数是**给别人看的**，残缺、易变、是为人类可读性设计的。
> 带状态的宿主通常会落一份**给机器看的**注册表（`~/.<app>/sessions/`、
> `state.json`、`*.pid`、运行时目录里的 socket 名）。**优先找它。**
> 判断标准：命令行是你**推**出来的，注册表是宿主**写**下来的——后者才是事实。
>
> 顺带一个可证伪的预测，验证你的修法对不对：**"我新建一个对话，它应该立刻变绿。"**
> 说得出这句话，才说明修的是根因；说"你多用一会儿应该就好了"是在糊。

### 2.3 ★ 认证到底怎么判的——读源码，别猜（claude 2.1.270 实测）

§2 开头那句"必须是子孙进程"，最早是**对照实验**得出的（同为子进程进得去、
`nohup` 出来的进不去）。后来把**判定函数本身**从二进制里挖出来了，结论一致，
而且给出了边界到底画在哪。判定就只有这一处（macOS 路径；压缩过的 JS，
变量名是它自己的）：

```js
async function Ge(pid, io){
  let token = await io.readStartToken(pid);       // 读「连接方」的启动令牌
  if (token === undefined) return "no-evidence";  // 读不到 → 没证据
  ...
  let chain = await io.readAncestors(pid);        // 读「连接方」的祖先链
  let mine  = process.pid !== 1 && chain.includes(process.pid);
  return mine ? "self" : "not-self";              // ← 我在这条链里吗
}
```

外层再包一层：

```js
let v = await verdictOf(verifiedPeerPid);
return v === "self" || (v === "no-evidence" && childTokenPresented);
```

**`chain.includes(process.pid)`** —— 连过来的进程，祖先链里必须有**收信的那个
会话进程**。这就是"必须是子孙"的原文出处，也是 `nohup &` 失败的原因：被 launchd
收养，从链上掉了。

`verifiedPeerPid` 是**内核读出来的**（`SO_PEERCRED` / `LOCAL_PEERPID`），
不是 payload 里写的——**伪造 payload 没用**。它的官方注释里还有一句关键的：

> …which for **relayed traffic (e.g. a daemon forwarding on another session's
> behalf)** is the relay, not the message's author.

**"一个 daemon 代表另一个会话转发"是它自己考虑过的形态。** 所以想从外面写进某个
会话，正规做法是**在那个会话里放一个子孙进程当中继**，而不是硬连它的 socket。
这也正面解释了 §2 那条限制：一个 bridge 只能写它自己出生的那一个会话。

#### 顺带两条协议细节

- **帧里必须带 `session_id`**，对不上直接丢：
  `[uds-messaging] Dropping N message: session_id mismatch (got "…", expected "…")`
- **有个开关 `crossSessionInbound`**：`accept`（默认）/ `hold`。设成 `hold`，
  进来的跨会话消息会被**扣下来等收信人审批**（原文：*"held for the recipient
  user's approval … permission-mode parity"*）。分 user / repo / org 三层，
  **repo 和 org 只能往紧的调**。排查"发出去没反应"时先看它。

#### 那条"后门"，别走

`v === "no-evidence" && childTokenPresented` 意味着：读到连接方的启动令牌失败时，
改看有没有带"子令牌"。理论上有条绕过去的路。

**不走**，两个理由：那是给"拿不到证据"兜底的**降级路径**，不是正常入口；而且它
绕过的正是 harness **故意设的那道边界**——"同一用户下的别的进程不能随便插进你的
会话"。那是保护，不是障碍。正规中继（子孙进程）**不需要绕任何东西**。

> **通用**：逆到判定函数就停手，别再往下找绕过口。**知道边界画在哪，才能在边界
> 内把事做成**；找到后门只能让你做出一件随时会被堵掉的事。

### 2.4 ★ 怎么一眼认出「对端身份」这道门（Codex 实测）

§2.3 是 Claude 的路子：**读源码**。Codex 是 Rust 二进制，没源码可读，但它的
认证方式有个**很好认的指纹**，值得单独记下来：

| 你发什么 | 连接会怎样 |
|---|---|
| **什么都不发**，就这么连着 | 一直连着，不关 |
| 发一个字节垃圾 | **立刻 EOF**（实测 0.0 秒） |
| 发半行 JSON | 立刻 EOF |
| 发一个**格式完全正确**的 `initialize` | 立刻 EOF |

**关键不是"被拒"，是"拒得有多快"。** 协议解析器得读够字节才能判断格式对不对，
所以"半行 JSON 被拒"和"合法 JSON 也被拒"**一样快**，就说明它**根本没看内容**
——它在看**是谁连上来的**（对端 pid / 代码签名）。这时候继续猜帧格式纯属白费。
**对比**：Claude 那个 socket 是"连上、收下、然后自己判断"（§2.3），读源码才
看得出边界；Codex 这个连字节都不收，**行为本身就招了**。

> **通用判据**：**把"被拒的速度"当探针。** 拒绝跟内容复杂度无关，就是身份门，
> 换多少种 payload 都没用；拒绝随内容变化，才是协议门，值得继续逆。

**认出这道门之后，别自己实现客户端**——让**它自己那份签过名的二进制**替你说话：

```bash
/Applications/ChatGPT.app/Contents/Resources/codex queue --thread <id> --message "…"
```

**顺带一个必验的坑：版本要对上。** 同一个 App 可能在两处放着不同版本的二进制
（这里 PATH 上是 0.151.0，App 自带的是 0.154.0-alpha）。门既然按对端身份开，
**发命令的就得是 App 自己那份**，别用 PATH 上那个——要么被拒，要么跟正在跑的
app-server 说不到一块去。先 `codex --version` 各查一遍。

---

## 3. 逆协议：别猜，去 grep 二进制

拿到 socket 不等于知道发什么。**别试错**，直接去宿主程序的二进制里找它的解析器。

Claude 是 Electron 打包的 JS，好办——直接对可执行文件做**定长字符串**搜索：

```bash
TARGET="$HOME/Library/Application Support/Claude-3p/claude-code/2.1.270/claude.app/Contents/MacOS/claude"
grep -aoF 'cross-session-message' "$TARGET" | head
```

（216 MB 的二进制，**别用会回溯的正则**，`grep -ao '.\{0,400\}'` 这种会跑到超时。
用 `-F` 定长串，或者用 Python `bytes.find()` 拿字节偏移再往两边读。）

找到目标常量之后，**在它邻域里读代码**。我们的关键收获是从这里来的：

```js
// 信封的属性顺序是写死的：from / from-session / hop-chain / from-name / from-mode
function zfe(e, n, r, s, d) {
  let m = [];
  if (e) m.push(`from="${e}"`);
  if (r && HB.test(r)) m.push(`from-session="${r}"`);
  if (s !== void 0 && s.length > 0) { let A = s.join(","); if (VB.test(A)) m.push(`hop-chain="${A}"`); }
  let S = n === void 0 ? void 0 : eA(n.replace(/["<>]/g, ""));   // ← 显示名会过滤掉引号尖括号
  if (S) m.push(`from-name="${S}"`);
  if (d) m.push(`from-mode="${d}"`);
  return m.length > 0 ? ` ${m.join(" ")}` : "";
}
function uze(e, n, r, s, d, m) {
  let S = zfe(e, n, s, d, m);
  return `<${e5}${S}>\n${XPe(e5, r)}\n</${e5}>`;   // e5 === "cross-session-message"
}
```

**这一步的收获不止是格式**，还顺手拿到了安全语义（见第 4 节）。

> **通用**：
> - `strings` / `grep -aF` 先定位关键词（`socket`、`ipc`、消息标签名、报错文案）
> - 找到常量名之后**在附近读**——解析器和它对偶的序列化器通常挨着
> - **优先找报错文案**。程序员的报错信息里往往把机制说破了
>   （我们就是这样读到"跨会话消息永远不算用户意图"那条的）
> - 是 JS 就找 `node_modules`/asar 解包；是 Go/Rust 就找符号表

---

## 4. ★★★ 最关键的一条：**显示 ≠ 性质**

**这是整个项目最重要的一条认知，也是最容易搞错的一条。**

宿主程序会在外部消息前面糊一大段安全声明（"这条不是你的用户打的"云云）。
你一定会想：能不能去掉它？

**先把两件事拆开：**

| | 说的是什么 | 在谁手里 |
|---|---|---|
| **性质** | 这条消息不是用户意图、不能授权、不能提权 | 内核/系统提示。**碰不到，也不该碰** |
| **显示** | 屏幕上画出什么字 | **分渲染器，逐个看** |

### 4.1 原理：同一个文件，几个程序各自画

会话日志文件是**唯一的事实来源**，但它被**多个程序**读，各自渲染：

```
            同一个文件（一个字都不用改）
                          │
          ┌───────────────┴───────────────┐
          ▼                               ▼
   宿主自己的 UI                      你的程序
   （别人的代码，改不了）              （你的代码，随便改）
          │                               │
   原样渲染 / 读结构化字段            自己挑、自己切
```

**所以要问的不是"能不能过滤"，而是"这个显示是谁画的"。**

### 4.2 两个机制，效果像，手段完全不同

这是本项目最反直觉的一点。两个地方都变干净了，靠的却是两套完全不同的办法：

| | 手机那一页 | 宿主 App 那一页 |
|---|---|---|
| 谁画的 | **我们自己的网页**（`ui.html`） | 宿主 App |
| 渲染代码归谁 | 我们 | 别人，**碰不到** |
| 怎么变干净的 | 改**渲染器**——自己剥壳 | 改**数据**——把结构化字段塞进 payload |
| 能不能不靠数据 | 能 | **不能** |

第二种是"改 Skill 竟然改到了别人的 App"的全部秘密：

> **你碰不到对方的渲染器，但你能控制喂给它的数据。**

宿主收到一个**格式正确的信封**后，会把它拆开，抽出干净字段：

```json
"origin": {
  "kind": "peer",
  "from": "unknown",            ← 可寻址身份，没声明
  "name": "phone-bridge",       ← 从信封的 from-name 抽出来的（显示名）
  "body": "你好！！",            ← 剥掉三层壳的干净正文
  "verifiedPeerPid": 4966       ← 内核给的，伪造不了
}
```

它的 UI 一直会写"有 `name` 就用 `name`、有 `body` 就用 `body`"——只是**以前这两样
都不存在**，只好退回糊原始正文。一旦信封格式对了，字段就有了，显示自己就干净了。

**这是可验证的**：加信封之前的所有消息，`origin` 里**没有** `name`/`body`（实测
41 条里只有加了信封的 2 条有）。所以那些老消息到现在还是糊的——同一屏里新旧
并排，一眼分得出。**这个预测本身就是验证。**

> **通用**：
> - 遇到"宿主糊了一段你不想要的东西"，**先别想删掉它**。先问：这段话是给**模型**
>   看的还是给人看的？给模型看的基本删不掉（也不该删）；给人看的，看是谁画的。
> - **你能控制输入，就能间接控制别人的输出。** 在 payload 里把结构化字段补齐
>   （身份、正文、来源），宿主很可能拿这些字段去渲染，绕开了原始文本。
> - **别去改宿主的代码/二进制。** 改了签名就废，升级就废，而且越界。
>   改数据是干净的、可撤销的。

---

## 5. 署名：身份要在 payload 里自己声明

默认情况下，外部消息在宿主 UI 上显示成 `unknown`——因为**没人声明**。显示名
得发送方自己在信封里写：

```
<cross-session-message from-name="phone-bridge">
正文
</cross-session-message>
```

**通用**：宿主常常会从 payload 里解析一个"显示名"或"发送者"字段。找它，填它。
这是零成本地让界面变得可读。

### 5.1 两个坑

**坑一：属性顺序写死的。** 是 `from` / `from-session` / `hop-chain` / `from-name`
/ `from-mode`，都可选，但**顺序不能错**。逆协议时顺手记下顺序（见第 3 节代码）。

**坑二：正文别转义。** 我第一版照抄了宿主的 `<` → `<\` 转义，结果用户打的
`</cross-session-message>` 被悄悄吃成了 `<\>`，**用户的话丢了字**。

不转义也不会更糟——解析用的是**贪婪匹配 + 行尾锚定**，正文里就算出现标签名也能
正确切出来。剥壳用这个（注意 `[\s\S]` 和 `^…$` 锚定）：

```python
_ENVELOPE_RE = re.compile(
    r"^<" + PEER_TAG + r'(?: from="[^"]*")?(?: from-session="[^"]*")?'
    r'(?: hop-chain="[^"]*")?(?: from-name="[^"<>\n\r]*")?'
    r'(?: from-mode="[^"]*")?>\n([\s\S]*)\n</' + PEER_TAG + r">$"
)
```

> **通用**：想让"正文里出现边界符"仍然正确，正确做法是**贪婪匹配 + 两端锚定**，
> 而不是转义。转义会静默改字——那是最坏的一类 bug（不报错、丢数据）。

---

## 6. 剥壳：只在你控制的那一层做

宿主会在外部消息外面套 **3 层**：

```
Another Claude session sent a message:            ← 宿主前缀
<cross-session-message from-name="phone-bridge">  ← 我们自己的信封
正文
</cross-session-message>
                                                  ← 空行
This came from another Claude session — …          ← 宿主声明
```

宿主的 UI 自己会拆（第 4.2 节）。**但我们自己的渲染器得自己拆**，不然手机上
显示的是用户自己刚发的一句话被三层英文糊住。

```python
def _strip_envelope(text):
    if text.startswith(_PEER_PREFIX):
        text = text[len(_PEER_PREFIX):]
    cut = text.rfind(_PEER_SUFFIX)      # ← 从**最后**一处切，见下
    if cut != -1:
        text = text[:cut]
    m = _ENVELOPE_RE.match(text.strip())
    return m.group(1) if m else text
```

两个细节：

- **后缀用 `rfind` 不用 `re.sub`**。`sub` 从左匹配，用户要是自己贴过一段一模一样
  的英文，会把他打的话一起吃掉。切最后一处才是宿主加的那个。
- **认不出来就原样返回。** 宿主哪天改了措辞，我们的前缀/后缀就找不到，那堆英文会
  重新漏出来——这是**纯显示问题**，坏也坏得安全。永远选"坏了就退回原样"，
  别选"坏了就丢东西"。

### 6.1 同一件事可能有多种落盘形状

Claude 这里踩到了：消息落成什么形状**取决于注入时会话忙不忙**。

| 时机 | 落成什么 |
|---|---|
| 空闲 | `type: "user"`，`origin` 在**顶层** |
| 忙（含任务通知、宿主排队的输入） | `type: "attachment"` + `queued_command`，`origin` 嵌在 **attachment 里** |

**两个分支都要处理**，否则半数的消息读不到（或者读到没剥壳的原始版本）。
`origin.kind` 也不止一种：`peer`（外部注入）/ `human`（宿主里排队的话）/
无（系统的任务通知）——**只有 `peer` 才该打"来自外部"的标记**。

> **通用**：逆出格式之后，**一定找一次"忙"和"闲"两种状态各注入一条**，对比落盘
> 形状。只测一种状态是逆不完整的，而且失败得很隐蔽。

---

## 7. 怎么验：只信"能证伪的预测"

这个项目里我断言错过一次（说宿主 UI 还糊着英文，实际已经干净了），教训是：
**看不见的东西不许断言。**

**过关的验证手段，从强到弱：**

1. **端到端实测**：真发一条，去日志里看记成了什么。
   ```bash
   # 日志里最后一条外部消息记成了什么
   python3 -c "
   import json; p='<会话>.jsonl'
   for l in open(p, errors='replace'):
       e=json.loads(l)
       if (e.get('origin') or {}).get('kind')=='peer': last=e
   print(last['origin'])"
   ```
2. **边界用例**：剥壳这种字符串处理，必须跑**恶意输入**——
   - 正文里就带标签名
   - 正文里带宿主的声明原文
   - 普通消息（一个字都不许动）
   ```python
   cases = [("<信封>\n正文\n</信封>…", "正文"),
            ("普通消息", "普通消息"),          # ← 必须原样
            ("正文里带 </cross-session-message> 标签", "…")]
   ```
   实测 6/6 通过才上。
3. **可证伪的预测**：说出一个"如果我的解释对，你就会看到 X；不对就看到 Y"
   ——比如"老消息应该还是糊的，新消息干净，同一屏并排"。让对方去看。
   **说得出预测，才说明你真的有解释，而不是在编。**
4. **最弱：读代码推断。** 可以，但必须标明是推断。

> **通用**：看不见的界面别描述。要么给对方一个能自己验的预测，要么说"这个我看不到"。

### 7.1 ★ 一个只有真机才会碰到的 bug：滑动窗口 + 只追加的渲染器

出站那一侧我返回的是**最后 N 条**（滑动窗口，防着几万条的对话把手机拖死）。
渲染器写得很自然：记住"已经画了几条"，下次从那个下标往后接着画。

```js
for (let i = knownCount; i < msgs.length; i++) { … }   // ← 错在这
knownCount = msgs.length;
```

**窗口一满，这个循环就永远空了。** 数组长度定在 N 不再增长，但每来一条新的，
前面就掉一条——**下标整体平移**，`knownCount` 追不上。现象是：界面从某一刻起
**完全冻结**，只显示到那一刻为止。用户看到的是"我刚发的话被吃掉了"。

阴的地方有三处：

- **开发时永远正常**。对话短于 N 时数组一直在变长，逻辑完全正确。只有窗口
  满了才坏，而那时你早就在测别的东西了。
- **看起来像别的原因**。用户会归因给当时的偶然事件（我这边是"换了流量之后"），
  而它其实只跟"对话够长了"有关。
- **服务端一切 200、日志干净**。数据一直在正确地返回，是客户端不用它。

**修法：让服务端给每条消息带绝对序号，客户端按序号对账**，而不是按长度差。
序号对不上（窗口滑走 / 换了会话）就整屏重画；对得上就从顶上删掉滑走的那几条、
后面补新的。既不会漏，也不会重复画。

**验法**：这个 bug 用眼睛看代码很容易放过，但**可以用最小 DOM 桩把真的渲染
函数跑起来**（Node 里给 `document` 造个假对象，喂几批构造好的滑动数据，断言
DOM 条数和首尾内容）。我这么测的：16 条断言，覆盖"滑 1 格 / 滑 5 格 / 序号倒退
（换会话）/ 重复数据 / 空列表"。

> **通用**：分页/限长的数据流**一定会滑动**，而"只追加"的渲染器假设它只增长。
> 这两件事天生不兼容，而且只在数据超过一页之后才暴露。**给每条数据一个稳定的
> 身份（序号或 id），按身份对账**——这是唯一在两种情况下都对的写法。

### 7.2 ★ 验证本身会写进真实数据里——开测之前先想清楚它留下什么

自起那条路我是用 `claude -p "reply with exactly: ok"` 手测的。两件事一起发生：

1. **`-p "…"` 给了参数，不等于它就不看 stdin 了。** 我把这条命令写在
   `python3 <<'PY'` 的 heredoc 里，CLI 于是**接着往下读 stdin**，把后面剩下的
   脚本原文当成了提示词的一部分粘上去——那条"用户消息"实测 1582 字符，开头是
   我的 prompt，后面是我的 Python 源码。**"我给了参数"和"它不再看 stdin"是两件
   互不相干的事。** 要隔离就显式隔离（`</dev/null`），别指望参数帮你挡。
2. **它把测试会话落进了用户的真实列表。** 那几次手测在
   `~/.claude/projects/<临时目录>/` 下建了 5 个**真会话**，用户手机上看见了，
   来问"你是不是新建了几个测试呀？"。**验证跑在别人的生产数据上。**

> **通用**：等验完再想"会不会留下痕迹"就晚了。动手之前先答一句：**这次验证会往
> 谁的、哪个目录里写什么？** 能用临时 HOME / 临时目录就先用——事后清理是补救，
> 不是方案。顺带：临时目录（`/tmp` 之类）下攒出来的会话要**主动收**，因为它会
> 混进用户的正常列表里冒充"一个对话"。

---

## 8. 各家现状

### Claude（已打通）

| | |
|---|---|
| 出站 | `~/.claude/projects/*/*.jsonl` |
| 入站 | `/tmp/cc-socks/<pid>.sock`，UDS + 换行分隔 JSON |
| 认证 | **进程树**——交互式会话严格查血统；自起的 stream-json 会话不查（§2.0） |
| 信封 | `<cross-session-message from-name="…">`，属性顺序写死 |
| 落盘 | 忙/闲两种形状，都要处理 |
| 署名 | ✅ `from-name` → `origin.name` |
| 显示 | 手机端自己剥壳；宿主 App 靠结构化字段自己干净 |

### Codex（已打通）

| | |
|---|---|
| 出站 | `~/.codex/sessions/*/*/*/rollout-*.jsonl` |
| 入站 | `codex queue --thread <id> --message <text>`（**用 App 自带那份二进制**） |
| 认证 | **对端身份**——`ipc.sock` 拒任何字节（§2.4）；所以走它自己的 CLI，不自己拼协议 |
| 落盘 | 一条**普通用户消息**，跟手打的逐字段一致（`content_item_kinds: ["user.text"]`） |
| 署名 | ❌ 没有信封这一层，也不需要——它本来就是"像本人打的" |
| 显示 | 剥掉 `<image …>` / `</image>` 包装行；过滤注入的上下文 |

**读的一侧两个坑（都是实测撞出来的）：**

- **行 id 用 `session_meta.payload.id`，不是 `session_id`。** 子代理（guardian）
  也各写自己的 rollout，`session_id` 指向**父**会话、`id` 才是自己——拿
  `session_id` 当行 id，一个会话挂的三十个子代理会全塌成同一行（实测 80 行只剩
  37 个唯一 id、还互相顶掉）。子代理不进列表：它们是内部机器。
- **一个线程横跨多个 rollout 文件**（`history_mode: paginated`）：resume / 重开
  窗口会**新起一个分段文件**，里面只有当时那一段窗口。实测最极端的一个线程 13 段，
  而**最新那份只有 1 条消息**——只看最新文件的话，用户最常接着聊的会话在手机上
  反而几乎空白。按线程把各段拼起来（以最新为底往前补，只补没出现过的，撞上就停）。
  **别用"整段去重"**：那会把用户真的重复发过的同一句话吞掉一条。

**顺带纠正一条早先的猜测**：`ipc.sock` 不是"要握手"，是**认对端身份**（§2.4）；
而且**根本不用进去**——`queue` 就是正门，还不用改持久配置。

**还有一条现成的只读索引，但没用它**：`codex migrate-rollouts --json`（不加
`--apply` 只报告）给出每个线程的 `thread_id` + `rollout_path`。一次要 1.5 秒，
而且**不给 `cwd` / `originator` / `thread_source`**——那三个恰好是过滤子代理、
判断"能不能发"要用的。所以出站还是自己扫盘：首批 0.85 秒，之后**按文件缓存头部**
（第一行 `session_meta` 不会变），重扫 0.05 秒。**两条都实测过才这么定，别退回去
调那个 CLI。**

---

## 9. 一张纸的流程

接一个新 Agent，照这个顺序走：

1. **读**先做通（扫日志，处理"忙/闲"两种形状，过滤掉非人类输入）
2. 翻**官方远程控制**开关，实测能不能用
3. 找**本地 IPC**；找到后**先验认证方式**（真发一条，别信 socket 的返回值）
4. **grep 二进制**逆协议：找常量名、找报错文案、读相邻的解析器/序列化器
5. 搞清楚**宿主会从 payload 里解析哪些结构化字段**（身份、正文、来源）
   ——把能填的都填上，显示自己就干净了
6. 自己的渲染器上**只剥显示，不动投递**；认不出来就原样返回
7. **验证**：端到端实测 + 恶意输入边界用例 + 说得出可证伪的预测

**贯穿始终的一条**：显示 ≠ 性质。壳能剥，性质剥不掉。
手机上一颗干净的蓝色气泡，仍然不是用户的授权——界面得诚实地说明这一点。

---

## 10. 选通道：一张决策表

前面几节讲的是"某条路怎么打通"。真正上手时只有两个问题：
**这个对话有没有活进程**、**我能不能站进它的进程树**。

> **这两问是 Claude 的形状，不是通例。** Codex 两个问题都不用问：它的入口
> （`codex queue`）由自己的守护进程处理，**不看发信人站在哪棵树里**，窗口没开
> 也能发（§8）。接新 App 时先问一句"**有没有一个不挑发信人的官方入口**"——
> 有的话，下面这一整套（中继、进程树、N 个地址）就都不必存在。

| 目标对话 | 有活进程？ | 走哪条路 |
|---|---|---|
| **前端自己**待着的那个 | 有 | **UDS 注入**。发信方必须是**那个进程的子孙**，否则静默丢弃（§2.0） |
| **别的**正开着的会话 | 有 | **中继**——那个会话里得有我们一个人，因为只有它站在树里。没有中继就是**不能发**，明说原因，别静默失败 |
| 任何**历史对话**（没开着） | 没有 | **不能发**，只读。技术上还有"自起"那条路（§1.1），但默认关着：它换来的只是这一个边角场景，代价却是"另起一个脑子" |

**这张表是代码自动走的**：`adapters/claude.py` 的 `_route()` 按这几行分支，
列表里每个对话各算一遍，用户不用挑、也不会看到两个地址。**算出来不能发的那种，
手机上会整行变灰并说清楚怎么办**——这是有意的：早期版本会静默失败（§2）。

于是整个系统只需要维护两种进程：

- **A —— 绑在活会话上的中继。** 每个"想从手机写"的会话一个。
  它**必须待在那个会话的进程树里**，这是全项目唯一有门槛的地方。
  怎么让一个进程出现在别人的树里？只有三条路：**手动在那个会话里起**、
  **hook 起**（要动 `settings.json`）、**MCP server**（要动配置）。
  **进程树没法嫁接**——不能把已有进程挂到别人的树下。
  这也是唯一一个"要不要改客户配置"的取舍点。目前选了"手动"，代价是把
  提示做成一句话（"在那个对话里说一句『启动 phone-bridge』"），让用户不用记命令。
- **B —— 自己生的 headless 会话。** 用它去够那些没有活进程的历史对话，
  一条对话一个 `--resume`，想开几个开几个。它自己也会开 socket，
  但 **stdin 更简单**，不用碰认证。**现在默认不用**（见 §1.1 开头）。

**A 不应该占端口。** 这一点值得单独说，因为它决定了对外的形状：
资格不能共享（一个进程只有一个父进程），所以 A 必然是"每个会话一个"；
但如果每个 A 都开一个 HTTP 端口，用户就要面对 N 个地址——**这是设计的错，
不是认证的错**。让 A 只听一个 **UDS 文件**（`<dir>/<sessionId>.<pid>.sock`），
前端就变成唯一一个地址：读是全局的（谁都能读日志），写按 sessionId
**自动转发**给对应的 A。用户永远只见一个端口，A 的存在感是零。

发现 A 的办法和发现会话一样——**让写的人把事实写进文件名**（自己的 pid），
读的人只做 liveness 检查：死了的 A 留下的 socket 残骸，pid 一查就露馅，
顺手删掉，不用任何清理机制。

> **通用**：凡"某个能力只能被特定进程行使"的限制（权限、句柄、进程树、
> 单例资源），别把限制泄漏成用户界面的形状（N 个端口、N 个地址、N 个配置）。
> **在限制之内派一个代理，然后用文件/注册表把代理变成可发现、可寻址的**，
> 用户那边就还是"一个入口"。限制是实现的，不是产品的。

**什么时候会打架**：桌面端打开了某个对话（有了 A），而 B 也正指着同一个
sessionId。两个写入者在抢**同一份文件**（§1.1 事实 1），必须收掉一个——
**收 B**，因为 A 那边是用户正在看的窗口。

**所以选通道的规矩是"有 A 就不许 B"，而且这条判断可以完全自动**：查
`~/.claude/sessions/*.json` 里那个对话有没有活进程，有就走注入/中继，没有才自起。
用户那边不用挑。但**自起是"代班"，不是"常驻"**：手机那头聊完就没人看着它了，
得给一个闲置 TTL 并定期打扫（这里 15 分钟），别让它攒成一群没人管的脑袋。

**发现层**：`~/.claude/sessions/*.json` 就是答案，别退回去用 `ps` 扫（§2.2）。
3～5 秒轮一次足够，字段里有 `pid` / `sessionId` / `status`(idle|busy) /
`messagingSocketPath` / `cwd`。**看谁活着这件事不需要 hook、不需要改任何配置**；
需要配置的只有"在别人的树里起一个进程"这一件。

> **通用**：**可见性几乎总能免配置拿到**（注册表、日志、状态文件、socket 目录）；
> **写入权限才是要花代价的那一半。** 把这两件事拆开设计，别让难的半边
> 污染简单的半边——也不要用"要不要改配置"去否定整个方案，它只影响其中一条路。

---

## 11. Windows 实测：命名管道 + token（2026-09-17）

这一节记的是 Windows 与 macOS 不同的那条入站边界。结论先写：**Windows 不需要中继。**
一个不在目标 Claude 会话进程树里的进程，只要拿到该会话的 token，就能直接写它的命名
管道。实现上按平台分叉，macOS 的 UDS、进程树认证和中继全部保留。

### 11.1 命名管道能发现

命令：

```powershell
[System.IO.Directory]::GetFiles("\\.\pipe\") | Sort-Object
```

输出里有：

```text
\\.\pipe\codex-ipc
\\.\pipe\LOCAL\cc-msg-<session-hash>
```

Claude 的会话登记在 `~/.claude/sessions/<pid>.json`。Windows 活动会话会出现
`messagingSocketPath`，值就是上面的 `\\.\pipe\LOCAL\cc-msg-*`。同一目录还有一份
`<pid>.<hash>.key`：

```json
{
  "peerToken": "<token>",
  "procStartFt": "134340867377997082",
  "pidDomain": "win32:<host>"
}
```

`procStartFt` / `pidDomain` 用来防止 pid 复用后误用旧 token。`claude.py` 用
`session.json` 里的 `procStart` / `pidDomain` 校验 `.key`，只把 token 留在内存里，
不打印、不落盘。

### 11.2 token 能不能让树外进程写进去

用一个隔离的 Claude Code 会话验证，`USERPROFILE` 指到临时目录，并显式给会话一个临时
命名管道：

```powershell
$env:USERPROFILE="$PWD\.claude-probe-home"
python .claude-probe\run_claude_inbox_probe.py
```

当时会话登记和调试日志显示：

```text
CLAUDE_PID=9948
PIPE=\\.\pipe\LOCAL\cc-msg-cafd3978f3b4a35aba58d10380c0d295
[uds-messaging] Listening: \\.\pipe\LOCAL\cc-msg-cafd3978f3b4a35aba58d10380c0d295
[uds-messaging] Inject messages (auth line REQUIRED here; a pipe is not reachable via socat/AF_UNIX)
```

随后用一个独立 Python 进程调用适配器，不把它放进 Claude 会话的进程树：

```powershell
$env:USERPROFILE="$PWD\.claude-probe-home"
python -c "from adapters.claude import ClaudeAdapter; ad=ClaudeAdapter(); print(ad.send('22222222-3333-4444-8555-666666666666', 'WINDOWS_ADAPTER_E2E_20260917_B'))"
```

原始返回：

```text
{'ok': True, 'via': 'windows-pipe'}
```

Claude 自己记下的是：

```text
[uds-messaging] Routed user message to queue (priority=next):
<cross-session-message from-name="phone-bridge">
WINDOWS_ADAPTER_E2E_20260917_B
```

再从适配器读回：

```text
{'role': 'user', 'text': 'WINDOWS_ADAPTER_E2E_20260917_B', 'via': 'peer', 'i': 4}
```

这组对照回答的是关键问题：**不是“管道连上了”，而是消息真的被会话接收并写进日志。**
Windows 上 token 代表了入站资格，进程树不是判据。因此 bridge 可以直接服务多个活动
Claude 会话，不需要每个会话再起一个中继。

> 验证时发现，Codex 自带沙箱里的命名管道访问会返回 `EPERM`，官方 Node 客户端也一样。
> 同一操作在普通 Windows 进程里成功。实际运行 `bridge.py` 时要用普通终端，不要放进
> 这个沙箱。

### 11.3 Codex 的门还在

Windows 上的 Codex 桌面版自带 CLI，`queue` 子命令没有消失：

```powershell
$c = (Get-Command codex).Source
& $c --version
& $c queue --help
```

输出：

```text
C:\Users\onwar\AppData\Local\OpenAI\Codex\bin\<build>\codex.exe
codex-cli 0.155.0-alpha.2.6
Queue a message for an existing session
Usage: codex queue [OPTIONS] --thread <THREAD> --message <TEXT>
```

用一个临时线程实发后返回：

```text
Queued message 01a0ad60-ec6a-77d0-ba09-bb8a0f816592 for thread 01a0ad60-74cb-7450-bd14-50758767bac4
```

临时线程随后用 `codex delete --force` 删除。结论是 Codex 继续调用官方 `queue`，
不自己连 `\\.\pipe\codex-ipc`，也不改 Codex 的持久配置。

### 11.4 平台分叉点

- `adapters/claude.py`：Windows 从会话登记和 `.key` 读取管道/token，发送两行
  `auth` + `user`；macOS 原路径不变。
- `adapters/codex.py`：Windows 发现官方 `codex.exe`，调用 `queue`；macOS 继续使用
  ChatGPT.app 自带二进制。
- `bridge.py`：Windows 二维码写到系统临时目录，Tailscale 使用 Windows 安装路径，
  不套 launchd reparent 判据，`--relay` 直接提示不需要中继。

### 11.5 Windows 启动链复盘（2026-09-17）

Windows 上首次实测又暴露了两个不属于"端口能不能通"的问题：

- Tailscale 在 `D:\system_app\Tailscale\tailscale.exe` 运行，但 CLI 访问
  `\\.\pipe\ProtectedPrefix\Administrators\Tailscale\tailscaled` 被拒绝，返回
  `Access is denied`，所以 `tailscale ip -4` 失败。适配器不能据此判断 Tailscale
  不存在；CLI 失败后还要扫描本机 IPv4，并从 `100.64.0.0/10` 段识别 Tailscale
  地址。实测拿到 `100.118.186.23`。
- `qrcode` 自动安装确实被执行，但受限进程访问不到包源，pip 返回
  `No matching distribution found for qrcode`。旧实现把 stderr 丢掉，用户只看到
  `No module named 'qrcode'`。现在保留 pip 最后一行错误，并在普通网络环境下允许
  `pip install --user qrcode` 成功。

这两条说明：启动成功后还要验证三个信号——**绑定的远端地址、实际生成的二维码文件、
以及 `/api/sessions` 返回的正常响应**。只检查进程没退出不够。

### 11.6 启动权限会沿发送链传递（2026-09-17）

Windows 的 Claude 管道探针和 Codex `queue` 都在普通权限下成功，但把 `bridge.py`
放进 Codex 工具沙箱后，Codex 发送失败。手机端看到的原始错误是：

```text
WARNING: failed to clean up stale arg0 temp dirs: 拒绝访问。 (os error 5)
WARNING: proceeding, even though we could not create PATH aliases: 拒绝访问。 (os error 5)
Error: failed to initialize state database:
failed to initialize sqlite local db at C:\Users\onwar\.codex\state_5.sqlite:
attempt to write a readonly database
```

原因不是 `queue` 子命令或线程 ID，而是 `bridge.py` 从受限沙箱启动后，
`subprocess` 生成的 `codex.exe` 继承了同一套文件系统限制。它连读取 Codex
状态库都失败，因此消息根本没进入 app-server。修正方式是停止沙箱里的服务，
改从普通 Windows 用户进程启动：

```powershell
python bridge.py --port 8971
```

再用同一个 HTTP 入口发送：

```powershell
$body = @{
  app = "codex"
  sid = "01a0ad7d-e031-7bc2-bc82-eb51eee5b54e"
  text = "我说的是佛山和深圳！"
} | ConvertTo-Json

Invoke-RestMethod `
  -Uri "http://127.0.0.1:8971/api/send" `
  -Method Post `
  -ContentType "application/json; charset=utf-8" `
  -Body $body
```

普通权限下返回：

```json
{
  "ok": true,
  "via": "codex queue",
  "detail": "Queued message 01a0ad97-aa96-7152-8ced-c4c2a43ec99b for thread 01a0ad7d-e031-7bc2-bc82-eb51eee5b54e."
}
```

随后 `/api/messages` 能看到用户消息和 Codex 的回复，证明修复不是只看退出码，
而是端到端落盘成功。

> **通用**：进程间调用会继承启动者的权限。探针和客户端成功只能证明协议通；
> 正式服务必须从真实运行权限启动，再重跑一次端到端发送。Windows 上尤其要检查
> 用户目录（`~/.codex`、`~/.claude`）是否在沙箱的写入白名单里。
